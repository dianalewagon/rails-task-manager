<h1>Tasks</h1>
  <ul>
   <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>
      | <%= link_to 'Delete', destroy_path(task), method: :delete %></li>
      <% end %>
  </ul>
  <p><%= link_to 'Add new', tasks_new_path %></p>




